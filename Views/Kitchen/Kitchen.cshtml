
@{
    ViewData["Title"] = "Kitchen";
}

<h1>Køkkenets side</h1>
Her kan du finde informationer om følgende på en givet dato: det forventet antal gæster i alt samt hvor mange voksne og børn. <br/><br/>Du kan desuden finde info omkring antal voksne og børn checket ind.
<br/>Samt hvor mange, der endnu ikke er checket ind i alt, herunder hvor mange voksne og børn.
<br/>


<form action="/action_page.php">
    <label for="givenDate">Vis information for denne dato:</label>
    <input type="date" id="givenDate" name="givenDate">
    <input type="submit" value="Vis information">
</form>

<br/>
<button type="button" onclick="alert('Informationen er opdateret!')">Opdater information!</button>

<table class="table">
    <thead>
        <tr>
            <th scope="col" id="group">Group</th>
            <th scope="col" id="expected">Total guest expected</th>
            <th scope="col" id="checkedIn">Checked in</th>
            <th scope="col" id="notCheckedIn">Not checked in</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <th scope="row" headers="group">Adults</th>
            <td>
                @Model.ExpectedAdults
            </td>
            <td>
                @Model.CheckedInAdults
            </td>
            <td>
                @(Model.ExpectedAdults - Model.CheckedInAdults)
            </td>
        </tr>
        <tr>
            <th scope="row" headers="group">Children</th>
            <td>
                @Model.ExpectedChildren
            </td>
            <td>
                @Model.CheckedInChildren
            </td>
            <td>
                @(Model.ExpectedChildren - Model.CheckedInChildren)
            </td>
        </tr>
        <tr>
        </tr>


    </tbody>
    <tfoot>
        <tr>
            <td headers="group">Total</td>
            <td>
                @(totalExpected = Model.ExpectedChildren + Model.ExpectedAdults)
            </td>
            <td>
                @(totalCheckedin = Model.CheckedInChildren + Model.CheckedInAdults)
            </td>
            <td>
                @(totalExpected - totalCheckedin)
            </td>

        </tr>
    </tfoot>
</table>
